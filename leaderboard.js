(function(){const KEY='phuketmunch_leaderboard';function load(){try{return JSON.parse(localStorage.getItem(KEY))||[];}catch(e){return[];}}function save(l){localStorage.setItem(KEY,JSON.stringify(l));}function top(n=20){return load().slice(0,n);}window.Board={submitScore(n,s){const l=load();l.push({name:n||'Anon',score:s,ts:Date.now()});l.sort((a,b)=>b.score-a.score);save(l.slice(0,50));},renderLeaderboard(){const ol=document.getElementById('leaderboardList');ol.innerHTML='';top(20).forEach((r,i)=>{const li=document.createElement('li');const dt=new Date(r.ts).toLocaleDateString();li.textContent=`${i+1}. ${r.name} — ${r.score} (${dt})`;ol.appendChild(li);});}}})();